# qwen3 q projection GEMM  M(batch)=128 K(input)=1024 N(output)=2048
# projection-level LLM workload (attention score/context are out of scope)
[net]
height=1024
width=1
channels=1
batch=128

[data]
dataset=imagenet
test=datasets/gemm/test.lst
labels=datasets/gemm/labels.lst
top=1

[connected]
output=2048
activation=linear

[softmax]
groups=1

[cost]
type=l2
