# gemma_full q projection GEMM  M(batch)=128 K(input)=1536 N(output)=4096
# projection-level LLM workload (attention score/context are out of scope)
[net]
height=1536
width=1
channels=1
batch=128

[data]
dataset=imagenet
test=datasets/gemm/test.lst
labels=datasets/gemm/labels.lst
top=1

[connected]
output=4096
activation=linear

[softmax]
groups=1

[cost]
type=l2
