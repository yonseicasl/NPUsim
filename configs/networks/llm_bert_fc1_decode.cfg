# bert fc1 projection GEMM  M(batch)=1 K(input)=768 N(output)=3072
# projection-level LLM workload (attention score/context are out of scope)
[net]
height=768
width=1
channels=1
batch=1

[data]
dataset=imagenet
test=datasets/gemm/test.lst
labels=datasets/gemm/labels.lst
top=1

[connected]
output=3072
activation=linear

[softmax]
groups=1

[cost]
type=l2
