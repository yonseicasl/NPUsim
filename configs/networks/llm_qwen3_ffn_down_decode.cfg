# qwen3 ffn_down projection GEMM  M(batch)=1 K(input)=3072 N(output)=1024
# projection-level LLM workload (attention score/context are out of scope)
[net]
height=3072
width=1
channels=1
batch=1

[data]
dataset=imagenet
test=datasets/gemm/test.lst
labels=datasets/gemm/labels.lst
top=1

[connected]
output=1024
activation=linear

[softmax]
groups=1

[cost]
type=l2
