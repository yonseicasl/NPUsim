# gemma_full o projection GEMM  M(batch)=128 K(input)=4096 N(output)=1536
# projection-level LLM workload (attention score/context are out of scope)
[net]
height=4096
width=1
channels=1
batch=128

[data]
dataset=imagenet
test=datasets/gemm/test.lst
labels=datasets/gemm/labels.lst
top=1

[connected]
output=1536
activation=linear

[softmax]
groups=1

[cost]
type=l2
