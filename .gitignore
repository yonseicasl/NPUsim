result/

raw-mapping/

library/libnpusim.so

mappings/

models/model

models/datasets

models/weights

configs/

ext/

__pycache__/

*.o

# Simulator per-run output files (regenerated by every run)
models/*.txt

# SCALE-Sim per-layer traces (multi-GB); only the *REPORT*.csv summaries are committed
validation/phase1/out/*/layer*/

# Internal design/assessment/issue docs (local only, not published)
plan/
assessment/
implementation/
issue/

# Section D decompression sweep outputs (regenerate via run_decomp_sweep.sh)
validation/decomp/out/
validation/decomp/results.csv
